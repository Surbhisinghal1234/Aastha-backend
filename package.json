{
  "name": "company-management-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm start --prefix frontend",
    "server": "node server.js --prefix backend",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.15" 
  }
}
